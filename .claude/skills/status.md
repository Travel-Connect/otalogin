# /status

プロジェクトの状態を確認するスキル

## 使用方法

```
/status
```

## 実行内容

<status>

以下の項目を確認して報告してください:

1. **Git状態**: `git status` で未コミットの変更を確認
2. **開発サーバー**: ポート3000-3010で動作しているプロセスを確認
3. **環境変数**: `.env.local` の設定状況（値は表示しない）
4. **Supabase接続**: 接続可能かどうか

結果を表形式で見やすく報告してください。

</status>
